import React from "react";

const Home = () => {
  return (
    <div>
      <h1>Home Page</h1>
      <p>
        import BrowserROuter, Router, Switch. <br />
        Router component wraps all our index returns.ROute converts component to
        pages. Switch makes sure only the first compoenent that matches url is
        rendered.
      </p>
    </div>
  );
};

export default Home;
